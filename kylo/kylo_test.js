'use strict';

describe('myApp.kylo module', function() {

  beforeEach(module('myApp.kylo'));

  describe('kylo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var kyloCtrl = $controller('KyloCtrl');
      expect(kyloCtrl).toBeDefined();
    }));

  });
});