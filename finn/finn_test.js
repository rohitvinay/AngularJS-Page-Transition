'use strict';

describe('myApp.finn module', function() {

  beforeEach(module('myApp.finn'));

  describe('finn controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var finnCtrl = $controller('FinnCtrl');
      expect(finnCtrl).toBeDefined();
    }));

  });
});